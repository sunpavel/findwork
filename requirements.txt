# Ядро findwork (скоринг, источники, пайплайн, Telegram-нотификации, авто-отклик
# через эмуляцию приложения HH) работает БЕЗ зависимостей — только стандартная
# библиотека Python 3.11+. Этот файл нужен лишь для опционального улучшения качества.

# Опционально: качественная адаптация резюме и сопроводительных через Claude API.
# Без него src/tailor.py использует детерминированный шаблон (всё работает, но текст проще).
#   pip install -r requirements.txt
anthropic>=0.40

# Опционально: авторизация под официальное мобильное приложение HH для авто-отклика
# (создание резюме + отклики, которые dev-API блокирует). Можно установить отдельно:
#   pipx install hh-applicant-tool   # затем `hh-applicant-tool authorize`
# hh-applicant-tool
