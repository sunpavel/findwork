# Ядро findwork (скоринг, источники, пайплайн, Telegram-нотификации, авто-отклик
# через эмуляцию приложения HH) работает БЕЗ зависимостей — только стандартная
# библиотека Python 3.11+. Этот файл нужен лишь для опционального улучшения качества.

# LLM (OpenAI/Anthropic) дёргаются по HTTP (src/llm.py) — SDK не обязателен.
# Пакет anthropic нужен только старому пути src/tailor.py с ANTHROPIC_MODEL через SDK.
# Без LLM-ключей tailor.py использует детерминированный шаблон.
anthropic>=0.40

# Генерация файлов резюме (PDF + DOCX) для assist-режима (facancy и пр.).
# Нужны для двухагентного пайплайна src/career_agent.py + src/resume_doc.py.
# Плюс системный шрифт с кириллицей: apt install fonts-dejavu-core
#   pip install -r requirements.txt
fpdf2>=2.7
python-docx>=1.1

# Опционально: авторизация под официальное мобильное приложение HH для авто-отклика
# (создание резюме + отклики, которые dev-API блокирует). Можно установить отдельно:
#   pipx install hh-applicant-tool   # затем `hh-applicant-tool authorize`
# hh-applicant-tool
